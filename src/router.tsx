import { createBrowserRouter } from 'react-router-dom';

const router = createBrowserRouter([
  { path: '/', element: <>{'path: /'}</> },
  { path: '/EXC365GD1', element: <>{'path: /EXC365GD1'}</> },
  { path: '/EXC365GD2', element: <>{'path: /EXC365GD2'}</> },
]);

export default router;
